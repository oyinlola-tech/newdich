node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.DS_Store
_legacy/
design.md
# Secrets — the deployed key lives in the host, not the repo
.env
.env.local

# Emitted by `npm run test:contact`
.test-build/
